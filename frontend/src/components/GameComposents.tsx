import React, {useEffect} from "react";

const GameComposents: React.FC = () => {

    useEffect(() => {
       console.log("pute")
    });

    return (
        <div>
            <h1>Game</h1>
        </div>
    );
};

export default GameComposents;